# Project Notes

## Build / Run
All app code is Rust; the cargo workspace lives in `src/` and produces the `astrofin` binary. Everything is driven through `just` — recipes are OS-gated via `[macos]`/`[linux]`/`[windows]` attributes, so the same command works everywhere:
```
just deps      # one-time: submodules, CEF download, macOS brew packages
just build     # build + stage a runnable tree in build/ (+ .app bundle on macOS)
just test      # run the workspace test suite (depends on build)
just run       # run with debug logging → logs to build/run.log
just run-mpv   # run the bundled mpv CLI directly (mpv-only debugging)
just clean     # remove build/ and dist/, cargo clean
just fmt        # format the workspace (cargo fmt)
just fmt-check  # check formatting without writing
just clippy     # clippy with -D warnings + unwrap/expect/panic denies
just lint       # fmt-check + clippy
just strict-lint # lint + clippy pedantic/nursery
just appimage build # [linux] build AppImage
just flatpak build  # [linux] build Flatpak bundle
just dmg            # [macos] build Apple Disk Image (.dmg)
just package       # [windows] build NSIS setup.exe + WiX .msi into dist/ (from the staged build/)
```
Platform-specific entry points live in `dev/linux/`, `dev/macos/`, `dev/windows/`, imported by the top-level justfile.

## Working rules for Claude
- **Delegate grunt work to Opus 5 subagents** (Agent tool, `model: "opus"`, fresh `general-purpose` agent): builds, test runs, installer/CI wiring, mechanical edits, verification sweeps. Keep design decisions, diff review and the final merge in the main session. A `fork` subagent runs on the main session's model and does not count as delegation.

## Before Committing
Run `just fmt` and `just lint` before every commit; both must pass clean (lint runs `fmt-check` + `clippy`, and CI rejects unformatted or lint-failing code).

## Architecture
- **CEF** (Chromium Embedded Framework) — hosts jellyfin-web as an embedded browser; handles JS-to-Rust IPC for player control commands and renders the UI as an overlay texture above the video layer. Multi-process: browser process (main app, owns CefBrowser), renderer process (V8/Blink), GPU process. IPC via `CefProcessMessage`. Bindings via `cef-dll-sys`; project glue lives in `src/jfn_cef`.
- **mpv** (fork in `third_party/mpv`) — video playback; the desktop client injects native shims to override browser media playback. mpv owns its own window + GPU; libmpv is used only for the control plane (properties/commands/events).
- Wayland subsurface for video layer (Linux). Platform crates: `src/macos`, `src/windows`, plus Wayland/X11 paths under `src/`.

## mpv Integration
- **Never call sync mpv API (`mpv_get_property`, etc.) from event callbacks** - causes deadlock during video init. Use property observation or async variants instead.

## mpv Event Flow
mpv is the authoritative source of playback state. All state (position, speed, pause, seeking, etc.) flows from mpv property observations outward to the JS UI and OS media sessions. The JS UI and MPRIS/macOS media sessions are consumers — they never determine playback state, they only reflect what mpv reports. This means things like rate changes, seek completion, and position updates come from mpv, not from JS round-trips or manual bookkeeping.

## Astrofin fork — status and working notes (2026-09-07)

This repository is **Astrofin**, a fork of andrewrabert/jellium-desktop (upstream remote `upstream`; `origin` pushes to both GitHub `TheHalfrican/Astrofin` and the self-hosted Gitea). Rebrand is DONE and merged to `main`: binary `astrofin[.exe]`, per-user dirs `%APPDATA%\astrofin` / `%LOCALAPPDATA%\astrofin` (with a one-shot import of an existing `jellium-desktop` profile), app id `io.github.thehalfrican.Astrofin`, env prefix `ASTROFIN_*`, Astrofin artwork (master `resources/brand/astrofin-icon.svg`, icons generated by `dev/tools/brand/build-icons.mjs`, fe9b7f1) and the hygiene renames (`Astrofin*` ObjC classes in `src/macos/src/init.rs`, the `AstrofinCefInput` window class on Windows). Only cosmetic residue remains (`~/jellyfin-deps` cache dir in the deferred macOS legacy workflow, the upstream wiki link in the bug-report template). See `docs/rebrand-plan.md` (exhaustive checklist + migration design + orchestrator decisions in §7). macOS verification plan: `docs/macos-plan.md`. UI redesign direction: `docs/design-brief.md` (PS5-in-space; tokens/CSS vars to come from Claude Design).

Done on `main`:
- Native file dialogs (`src/jfn_cef/src/client_impl/dialog.rs` → `Platform::open_file_dialog` → `src/windows/src/file_dialog.rs`), plus CEF 151.3.24 which contains the upstream OSR file-chooser crash fix. Together these resolve upstream #681. macOS/Linux fall back to a graceful cancel (no dialog yet).
- `instance_ipc`: Windows second-instance detection fixed (a taken pipe name surfaces as `PermissionDenied`, not `AddrInUse`).
- `dev/windows/env.ps1`: msys64 clang bin is prepended to PATH (a WinLibs mingw on PATH otherwise breaks bindgen's libclang load).
- Video modes (`docs/video-modes.md`, merged 2026-09-07): Auto / Live-Action / Animation / Off, switched live through libmpv. Auto resolves per title from `astrofin:` tags -> Animation/Anime genres -> library (`videoModeLibraries` in settings.json, hand-edited) -> live-action; Off clears every shader and restores mpv's default scalers, overriding mpv.conf. `--config-dir`/`--cache-dir` are re-exported as `ASTROFIN_CONFIG_DIR`/`ASTROFIN_CACHE_DIR` so the CEF renderer (which reads settings.json itself) sees the same profile.
- Windows installers (`just package` -> `dev/windows/package.ps1`, merged 2026-09-07): per-user NSIS setup exe + WiX 5 MSI built from the staged `build/` tree, each uninstalling the other kind first, wired into both Windows CI workflows. Details and the ARP/per-user MSI notes: `docs/build-windows-notes.md` Step 5. No code-signing certificate yet (hook: `ASTROFIN_SIGNTOOL`).

- Playback OSD finished under the theme (2026-09-08): motion on tokens, glass popups, skip/up-next/stats panels, trickplay bubble, star chapter markers, bar lifted for bright video. `backdrop-filter` cannot see the mpv window, so OSD fills carry the contrast on their own.
- Transcode indicator (`docs/transcode-indicator.md`, 2026-09-08): `src/web/playback-source.js` draws a Direct Play / Direct Stream / Transcoding chip in the OSD header from the play options, refines from `/Sessions` (reasons, accel type, speed, lead), toasts per the `transcodeNotice` setting (off/cpu/any). Red only for software encoding or measured throughput, never the configured accelerator alone. Force Transcoding now really forces (no DirectPlayProfiles).
- Playback Info shows mpv stats (2026-09-08): properties observed only while the panel is open, snapshot pushed to JS at most once a second; `getStats()` builds Video/Audio/Player categories.
- Upstream: PRs #686-#690 and the #643 comment posted 2026-09-08 (see memory `upstream-contributions`). Two pre-existing upstream failures on their `main` (clippy in `windows/src/input.rs`, the instance-ipc test) are fixed on their `iced-ui`.
- Bugs fixed 2026-09-08: input-plugin's "player cannot be null" on every restart and the "queue invalid" warning (`src/web/input-plugin.test.js` pins both).
- A-B repeat loop (`docs/ab-loop.md`, 2026-09-08): `[` / `]` / `\` plus a `repeat` button drive mpv's own `ab-loop-a`/`ab-loop-b` through a `playerAbLoop(set-a|set-b|clear)` IPC (mpv stamps each point from its own pts via `no-osd ab-loop`, since a JS-sampled time lags and disarms the loop); `src/playback/src/ab_loop.rs` observes both properties process-wide and pushes them to `window._nativeAbLoop`, and `src/web/ab-loop.js` draws the scrubber band, A/B pins and readout from that push alone. Points are cleared on every load/stop (mpv keeps them across files) and never persisted; seeking past B does not loop, which is mpv's documented behaviour.

macOS (2026-09-09): first build, tests and launch verified on the M3 Max MacBook; the run log, two fixes (bundled-MoltenVK pin in `src/macos/src/mpv_host.rs`, boot-time sync mpv reads moved off the main thread via `mpv_read_off_main` in `src/jfn_rust/src/app.rs`) and the open items (login/playback/video-mode checks, a connect-screen shutdown segfault in CEF's GPU thread, fixed by creating the never-navigated main layer at `about:blank` in `src/jfn_cef/src/client/browser_ops.rs`; `build-macos.yml` passes on GitHub; note this clone needed GitHub added as a second push URL) are in `docs/macos-plan.md` under "Verified on the MacBook". `dev/macos/setup.sh` now upgrades stale formulas from its own list; build with `/opt/homebrew/bin` first on PATH.

Release flow: `CHANGELOG.md` + workspace `version` in `src/Cargo.toml` (`0.2.0` and `0.3.0` released 2026-09-08 as tags `v0.2.0` / `v0.3.0`; main then moves to the next `-dev`); `just package` builds the installers from the staged `build/`.

Windows build on this machine (no `just`; PowerShell 7 is the Store build at `%LOCALAPPDATA%\Microsoft\WindowsApps\pwsh.exe`, not Program Files): put `%USERPROFILE%\.cargo\bin` on PATH, then `pwsh -ExecutionPolicy Bypass -File dev\windows\build.ps1` (libmpv via `dev\windows\build_mpv_source.ps1 -Arch x64` and CEF via `cargo xtask fetch-cef` are done once and cached). Lint/test: dot-source `dev\windows\env.ps1` first. Details and timings: `docs/build-windows-notes.md`.

CI: `.gitea/workflows/build-windows.yml` runs on the self-hosted host-mode act_runner on the developer PC (label `windows-latest`), with `.cache/`, `build/` and `third_party/mpv-install` junction-linked to a persistent cache under `C:\Users\NoahM\act_runner\cache\astrofin`. When `.gitea/workflows` exists Gitea ignores `.github/workflows`. On the GitHub fork, push-triggered runs did not fire; `workflow_dispatch` works (`gh workflow run build-windows.yml -R TheHalfrican/Astrofin --ref main`).

Memory growth during long playback (upstream #643): **measured, not reproduced** (`docs/memory-growth-findings.md`, 2026-09-08). Steady playback converges, and ten item starts, ten forced Live-Action/Animation chain switches and ten fullscreen toggles each leave private bytes and board VRAM flat. The gigabyte VRAM steps an earlier session attributed to `video_mode::apply_inner` re-writing `glsl-shaders` were an artefact of `\GPU Process Memory\Dedicated Usage`, which Microsoft (KB4490156) documents as wrong for destroy-and-recreate workloads — it reported 36 GB on a 24 GB card during the fullscreen run. Landed anyway as hygiene on `fix/shader-chain-vram`: memos in `video_mode.rs` and `api.rs` so an unchanged shader chain and an unchanged `background-color` are not re-written. The incidental finding worth keeping: mpv's ~2 renderer rebuilds per item come from our own `background-color` alternation on player open/close (it lives in `gl_video_conf`), not from the shader chain; the two colours genuinely differ, so removing those rebuilds needs a behaviour change, not a memo. The uptime gap is now closed too (§11, run L1, 2026-09-08): 90 min of auto-played 1080p HEVC episodes, then 30 min paused, then 10 min idle on Home, 131 min in one process. Main private is +4.9 MB/min for the first ten minutes and −0.03 MB/min for the next eighty, ±0.06 MB/min in every process while paused (the report claims ~19), and the tree peaks at 3 073 MB against the report's 3 485. The renderer heap ends 10 % smaller than at t=0, and the paint counter falsifies plan §2.1 outright — 2 paints/min while paused, 6 814/min idling on Home with private bytes *falling*. If it persists upstream, the next experiment is this same harness against upstream jellium-desktop's own release on this hardware, not another run on ours.

Known upstream issues worth reporting/PRing: the OSR file-dialog handler + ipc fix above; CI `build-windows.yml` appends msys64 to PATH (latent libclang shadowing); the Flatpak bundles GPL-3 text for a GPL-2 project; Windows memory growth during long playback (upstream #643) — worth posting the negative result and the KB4490156 counter trap, which is likely to have misled other reporters.
